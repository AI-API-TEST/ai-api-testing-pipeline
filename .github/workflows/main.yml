name: API Tests

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run API Tests
        run: |
          npx apidog-mcp-server@latest run openapi.json \
            --api-key ${{ secrets.APIDOG_API_TOKEN }}
